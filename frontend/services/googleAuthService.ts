/**
 * googleAuthService.ts
 *
 * ⚠️  This file is NOT used. Import `useGoogleAuth` from `./useGoogleAuth` instead.
 *
 * DEPRECATED — kept as reference only.
 * The backend has no Google auth route yet. When one is added:
 *   - POST to `${BASE_URL}/api/v1/auth/google` with { id_token: idToken }
 *   - Receive { access_token, refresh_token } and call saveTokens()
 */

// No exports — this file is intentionally empty until backend Google auth is implemented.